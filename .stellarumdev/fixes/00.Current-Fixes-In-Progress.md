> **Last synced:** 2026-05-06 — Move completed work into Master Plan III / MVP roadmap; keep this file as a punch-list of *what we intended*, not only open items.

1. Diff Card v2: Structured Hunks & Line-Number Gutters (Master Plan 2.14) [COMPLETED — `ffb8177`]
Goal: Overhaul the current inline chat diff cards from flat unified text into a structured, high-signal code comparison viewer.
Key Features:
Gutter margins showing real old/new line numbers (directly addressing your earlier request!).
Highlighting specific word/character differences within changed lines.
Collapsing unchanged blocks with standard fold markers (e.g. +45 lines... with click-to-expand).
Sleek header styling with file icons and instant reveal-in-tree actions.
Why: Turns the chat diff into a premium, trustworthy code-review tool where you can verify the agent’s actions at a single glance.
2. Chat History Persistence Architecture (Master Plan 2.12)
Goal: Move conversation logs from hidden local app data directory into a transparent, first-class .prism/history/ folder directly inside the project root.
Key Features:
Automatically initialize history files inside .prism/history/.
Update the History tab in Settings to scan and display logs from this location.
Add a sync button to optionally map history to custom cloud folders (Dropbox/iCloud) for cross-device persistence.
Why: Makes conversation transcripts portable, transparent, and instantly searchable with standard terminal tools (like grep).
3. First-Run Onboarding Wizard (MVP Gate 1.5) [COMPLETED]
Goal: Design a beautiful, minimalist onboarding sequence for new users upon launching the app.
Key Features:
- A premium glassmorphic modal series guiding the user to:
  - Select their initial project directory.
  - Securely connect their LLM API keys (Logic built programmatic, connection hidden).
  - Choose their "Primary Model."
Why: Replaces the initial blank slate with a welcoming, zero-friction path to first-turn value.
4. Toolbar Protocols Dropdown Menu (Master Plan 4.1 & 4.2)
Goal: Build a polished, interactive "Protocols" menu directly into the top application toolbar.
Key Features:
A dropdown listing pre-packaged developer sequences (e.g., "Security Audit", "Harden Loop", "Clean Refactor").
Exposes high-performance features in a single click, with custom tooltips showing current requirements and disabled states.
Why: Promotes discoverability of Prism’s advanced workflow chains so users don’t have to memorize commands.
5. Natural Language vs. Slash Command Parity (Master Plan 4.6)
Goal: Upgrade the agent's autonomous planning capabilities when chatting in pure English.
Key Features:
Allow the agent to self-trigger phase-planning, diagnostic checks, and verify passes based on conversation context (e.g. if the user says "this looks buggy, please review it").
Why: Ensures power-user slash commands (like /refactor or /protocol) become organic capabilities of Prism’s natural language mode, making the "just talk to it" experience incredibly powerful.

6. Contract-Change Consumer Enumeration (Safety Gate) (Phase 1.6a) [COMPLETED — `c2885f9`, hardened `36f809e`]
Goal: Prevent high-impact, silent agent regressions during code alterations.
Key Features:
- System prompt + post-condition checker that automatically scans the codebase for downstream usages when an agent updates an exported signature or public interface.
- Gating mechanism to ensure signature mismatch regressions are resolved within the same turn.
Why: Eliminates silent breaks in downstream dependencies.

7. Unified AI Usage Event Schema & Logging (Phase 5.1 & 5.2) [COMPLETED — schema + dedupe `75adf68`; billing UI still Phase 5]
Goal: Track all model calls across the application with standard event records.
Key Features:
- Standard unified schema fields: timestamp, user ID, model, tokens, calculated cost, and feature tags.
- Captures all surfaces including agent dialogue, verifier runs, and web searches.
Why: Lays the telemetry foundation for real-time customer billing and token-credit usage dashboards.

8. High-Contrast Code Blocks in Chat Panel (Phase 2.2) [COMPLETED — `80daf1e`]
Goal: Enhance the typography and readability of code blocks in the agent pane.
Key Features:
- Fine-tune visual border contrasts, modern background tokens, and precise code-syntax sizing.
Why: Provides a premium, high-fidelity developer aesthetic that is easy on the eyes.

9. Calibrate and Switch to Cheap Verifier (Phase 5.10) [COMPLETED — default `google/gemini-2.5-flash-lite` `63aeb3f`; optional `/verify on <model>` remains]
Goal: Lower the runtime cost of verification without sacrificing quality.
Key Features:
- Calibrate the verifier prompt and schema for high-competence, lower-cost models (e.g., Qwen3 235B or GLM-5).
- Achieves a 3x-5x reduction in overall token spend per turn.
Why: Reduces active developer and hosting costs dramatically.

10. Agent Search Bar Overlay (Cmd+F inside Dialogue) (Phase 2.11 in Master Plan) [COMPLETED — overlay + active-workspace scoping `e57e0bd`]
Goal: Implement native, responsive keyword traversal within past conversation logs.
Key Features:
- Seamless input overlay inside the agent dialogue stage.
- Real-time matches tracking and keyword cycling.
Why: Maximizes search capability over long-running debugging sessions.

11. Cross-Model Tab Memory Continuity (Session Handoff) (Master Plan 4.7) [COMPLETED — `adaba84`]
Goal: Preserve a durable per-tab working memory so switching models mid-session does not derail context, intent, or execution state.
Key Features:
Automatic handoff digest on model switch: current goal, constraints, decisions made, touched files, pending TODOs, and latest tool outcomes.
Tool-aware continuity (not prose-only): preserve important tool-call results and mode state so the next model inherits real execution context.
Budget-safe behavior for shorter-context models: compact handoff packets with progressive expansion only when needed.
Why: Model switching currently breaks continuity and makes "auto" routing feel unreliable; strong tab memory makes model swaps predictable and usable.

12. Auto-Router Control Plane via Gemini Flash-Lite (Master Plan 4.8) [COMPLETED — `9086807`]
Goal: Reintroduce auto model selection safely by using a low-cost evaluator model to classify task demands before execution.
Key Features:
Use `google/gemini-2.5-flash-lite` as a control-plane router only (not default deep-work executor).
Require strict structured output (JSON): task class, risk level, estimated context need, recommended model, confidence.
Apply deterministic guardrails after evaluation: context-window floor, no-thrashing, and high-stakes override to stronger models.
Why: Keeps auto-routing affordable and fast while preserving quality and continuity for refactors, audits, and long-context work.

13. Communication style rules as always-on preflight (`Communication_style.md`) [COMPLETED — `7779018`]
Goal: Load persona/voice guidance from the workspace rules bundle before each agent turn.
Why: Stable tone and terminology without manual repetition.