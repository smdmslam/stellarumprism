## FIXES IN PROGRESS (parallel copy)

**Why this file exists:** Same content intent as `00.Current-Fixes-In-Progress.md`, kept under a distinct name so merges between machines never silently overwrite your canonical list. When you are back at your main PC, delete or rename duplicates as you prefer.

---

Top 5 priority sequence (MVP-aligned):

### 1) Close Prism — canonical `prism/` workspace spine

**Scope:** Finish the rename/migration *story*: one visible workspace tree (`prism/`), docs and UI copy aligned, no lingering “hidden `.prism/`” as the primary mental model. Runtime may still read legacy `.prism/*` where shipped code keeps a fallback for old workspaces — that is implementation detail; **user-facing plans and Settings/help text point at `prism/`**.

**Acceptance:**

- Roadmaps (`00.MASTER-Plan-III.md`, `00.PRISM-MVP-ROADMAP.md`, cross-links in `fixes/`) use **`prism/`** for habits, history, second-pass audits, state, builds — not `.prism/` as the default documented path.
- Slash/help and Settings strings describe **`prism/skills/`**, **`prism/history/`** (and legacy fallback only where code still supports it).
- No duplicate “sources of truth” in planning docs: if Gate 3 still mentions history paths, they match actual scan roots (`prism/history` + optional legacy).
- Optional cleanup pass: align comment-only references in `src-tauri` doc comments from `.prism/` examples to `prism/` where they describe *new* writes (legacy sections may stay for migration clarity).

### 2) 4.1 / 4.2 — Protocol discoverability hardening

**Scope:** Toolbar/menu behavior for protocols is reliable and self-explanatory.

**Acceptance:**

- Disabled/inapplicable protocols show clear reasons.
- Requirement tooltips are accurate and visible.
- Menu behavior feels stable in normal use.

### 3) 1.1 — Substrate quality audit pass (`/build` and `/audit`)

**Scope:** Run ideation-to-completion scenarios; fix real breakpoints.

**Acceptance:**

- Repro checklist covers multiple realistic workflows.
- High-friction failures fixed or explicitly tracked.
- Short audit note for next sprint handoff.

### 4) Phase 5.3 — 5.8 — Billing foundation (commercial spine)

**Scope:** Minimal vertical for usage-to-billing coherence.

**Acceptance:**

- Pricing snapshotted at call-time and survives model price drift.
- Aggregation trustworthy for session/day/invoice windows.
- Stripe checkout + webhook skeleton exists for tier activation.

### 5) 5.11 — Default-cost strategy pass

**Scope:** Align defaults with auto-router and budget posture.

**Acceptance:**

- Default model/routing policy explicit (thrifty baseline, upscale rules).
- Settings/docs explain routing in plain language.
- Behavior matches cost policy without surprising users.
