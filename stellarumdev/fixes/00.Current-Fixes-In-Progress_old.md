## FIXES IN PROGRESS

Top 5 priority sequence (MVP-aligned):

1) 2.12 - Finish history + skills path migration to `prism/`
Scope: close migration end-to-end so user-facing data is visible and consistent.
Acceptance:
- History reads/writes default to `prism/history` with `.prism/history` fallback.
- Skills use `prism/skills` with legacy fallback where needed.
- Settings copy and slash/help text consistently reference `prism/*`.

2) 4.1 / 4.2 - Protocol discoverability hardening
Scope: complete protocol toolbar/menu behavior so it is reliable and self-explanatory.
Acceptance:
- Disabled/inapplicable protocols render with clear reasons.
- Requirement tooltips are accurate and visible.
- Menu behavior feels stable and intentional in normal use.

3) 1.1 - Substrate quality audit pass (`/build` and `/audit`)
Scope: run ideation-to-completion scenarios and fix real breakpoints.
Acceptance:
- Repro checklist covers multiple realistic workflows.
- Known high-friction failures are either fixed or explicitly tracked.
- Results summarized in a short audit note for next sprint handoff.

4) Phase 5.3 - 5.8 - Billing foundation (commercial spine)
Scope: minimal vertical to make usage-to-billing coherent.
Acceptance:
- Pricing is snapshotted at call-time and survives model price drift.
- Aggregation is trustworthy for session/day/invoice windows.
- Stripe checkout + webhook skeleton exists for tier activation flow.

5) 5.11 - Default-cost strategy pass
Scope: align defaults with auto-router and budget posture.
Acceptance:
- Default model/routing policy is explicitly defined (thrifty baseline, upscale rules).
- Settings and docs explain routing behavior in plain language.
- Verify behavior is consistent with cost policy and does not surprise users.